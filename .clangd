# clangd — не Xtensa-GCC: убираем флаги только для ESP8266.
# Системные заголовки подтягиваются через --query-driver в .vscode/settings.json.
CompileFlags:
  Remove:
    - -mlongcalls
    - -mtext-section-literals
    - -free
    - -fipa-pta
    - -falign-functions=*
